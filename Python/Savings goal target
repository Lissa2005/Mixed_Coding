# Input the target savings goal and the current savings amount
target_goal = float(input("Enter your target savings goal: "))
current_savings = float(input("Enter your current savings amount: "))

# Ask for the monthly saving amount
monthly_saving = float(input("Enter your monthly saving amount: "))

# Calculate how many months it will take to reach the goal
months_needed = (target_goal - current_savings) / monthly_saving

# Display the number of months needed
print("You will reach your savings goal in", months_needed, "months.")
